import SwiftUI

enum KeyForgeTheme {
    static let background = Color(red: 0.045, green: 0.052, blue: 0.061)
    static let surface = Color(red: 0.075, green: 0.085, blue: 0.098)
    static let elevatedSurface = Color(red: 0.095, green: 0.108, blue: 0.124)
    static let border = Color.white.opacity(0.09)
    static let accent = Color(red: 0.45, green: 0.84, blue: 0.62)
    static let muted = Color.white.opacity(0.55)
}
